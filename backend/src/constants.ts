export const ORDER_STATUS = {
  PENDING: 'pending',
  CONFIRMED: 'confirmed',
  CANCELLED: 'cancelled',
  COMPLETED: 'completed'
} as const;

export const CLASS_REGISTRATION_STATUS = {
  ACTIVE: 'active',
  CANCELLED: 'cancelled'
} as const; 